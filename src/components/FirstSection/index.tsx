import React from "react";

import "./index.scss";

interface FirstSectionProps {}

const FirstSection = (props: FirstSectionProps) => {
  return <div className="first-section"></div>;
};

export default FirstSection;
